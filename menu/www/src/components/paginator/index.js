import PaginatorComponent from './paginator.component';


let homeModule = angular.module('app.paginator', []);

homeModule.component('paginator', PaginatorComponent);

export default homeModule = homeModule.name;
